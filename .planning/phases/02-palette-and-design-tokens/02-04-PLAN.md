---
phase: 02-palette-and-design-tokens
plan: 04
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified:
  - _config.yml
  - .planning/phases/02-palette-and-design-tokens/verify.sh
autonomous: false
requirements: [TOKEN-01, TOKEN-04, TOKEN-05, TOKEN-06, GROUND-01, GROUND-04]

must_haves:
  truths:
    - "No theme toggle appears anywhere on the deployed site."
    - "Forcing data-theme=\"dark\" in DevTools changes no colour on the page."
    - "All seven pages show warm paper behind navbar, body, cards, code blocks and footer."
    - "No dark-grey bar is pinned to the bottom of any page; the footer is the bottom of the page."
    - "The horizontal scroll progress bar, which read the old accent, is gone."
    - "Every link in body copy is still identifiable as a link in a greyscale screenshot."
    - "The new token CSS is what the CDN actually serves, proven by grepping the served stylesheet."
  artifacts:
    - path: "_config.yml"
      provides: "enable_darkmode: false, enable_progressbar: false, footer_fixed: false"
      contains: "enable_darkmode: false"
    - path: ".planning/phases/02-palette-and-design-tokens/verify.sh"
      provides: "All static and --live rows green; red-by-design labels removed once true"
      contains: "--live"
  key_links:
    - from: "_config.yml enable_darkmode: false"
      to: "gem _includes/head.liquid:128 and header.liquid:127"
      via: "Liquid gate dropping theme.js, the dark Pygments sheet and the #light-toggle button"
      pattern: "enable_darkmode"
    - from: "_config.yml footer_fixed: false"
      to: "gem _includes/footer.liquid:14 footer.sticky-bottom"
      via: "swaps fixed-bottom for a bordered background-free footer; _layout.scss:27 zeroes body padding-bottom"
      pattern: "footer_fixed"
    - from: "local main"
      to: "https://phamhakhanhchi.com/assets/css/main.css"
      via: "one git push to origin main, then deploy.yml, gh-pages, CDN"
      pattern: "git push origin main"
---

<objective>
Flip the three config flags, push the whole phase as ONE deploy, prove the new CSS reached the CDN, and take the three verifications that genuinely need a human and a browser.

Purpose: `enable_darkmode: false` sits here and not in Phase 1 by hard constraint — it must land in the same deploy as the tokens, or a visitor whose browser has `data-theme="dark"` stored gets a genuinely broken, half-repainted page. This is also the phase's only contact with the remote, and the point where criteria 1, 2 and 3 stop being greps and become something a person looks at.

Output: `_config.yml` with three flags flipped, `origin/main` advanced by one push, `verify.sh --live` fully green, and a human sign-off on the seven-page sweep.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-palette-and-design-tokens/02-CONTEXT.md
@.planning/phases/02-palette-and-design-tokens/02-RESEARCH.md
@.planning/phases/02-palette-and-design-tokens/02-VALIDATION.md
@.planning/phases/02-palette-and-design-tokens/02-02-SUMMARY.md
@.planning/phases/02-palette-and-design-tokens/02-03-SUMMARY.md
@docs/DEPLOYMENT.md

<environment>
- `gh` is absent. The public GitHub REST API answers UNAUTHENTICATED (60 req/hr) on `hkchi-pham/phamhakhanhchi.github.io`. The org is `hkchi-pham`, NOT `phamhakhanhchi`.
- `/actions/runs?head_sha=` needs the FULL 40-character SHA. A short SHA returns `total_count: 0`, indistinguishable from "no run fired" — four polls were wasted on this in Phase 1.
- Measured deploy latency: workflow starts ~2s after the push, finishes ~71s, `gh-pages` advances ~97s, the new CSS is served ~124s. The last gap is GitHub's separate "pages build and deployment" run, which `deploy.yml` does not wait on. POLL; do not refresh once and conclude failure.
- `main.css` carries `Cache-Control: max-age=600` behind a proxy cache. ALWAYS fetch it as `curl -s "https://phamhakhanhchi.com/assets/css/main.css?cb=$(date +%s)"`.
- The `_tokens.scss` / `_custom.scss` output lands near the END of `main.css` (~97.6% down), not the top. Search the file; do not scroll to the top and conclude the deploy failed.
- `_config.yml` is NOT in `deploy.yml`'s `paths-ignore` denylist, so these edits deploy normally.
- `core.autocrlf=true`. Prettier: `--check --end-of-line auto`. NEVER `npx prettier . --write` — `prettier.yml` runs `prettier . --check` on every push with no path filter.
- Do NOT run `npm run test:visual` or `npm run test:visual:update`. The config hardcodes a `/al-folio` baseurl, targets deleted demo routes, and no snapshot baseline exists for this site. A red suite here is expected and must not be "fixed".
- Prettier rewrites a `**`-terminated glob inside a bold span. Keep such globs out of bold text in the SUMMARY.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: Flip the three config flags and take the phase static-green</name>
  <files>_config.yml, .planning/phases/02-palette-and-design-tokens/verify.sh</files>
  <action>
Three single-value edits in `_config.yml`. Each is a Liquid gate, not CSS — verified against the gem's includes.

`footer_fixed` at line 99, `true` to `false`. `footer.liquid:14` swaps `<footer class="fixed-bottom">` for `<footer class="sticky-bottom mt-5">`; `default.liquid:36` adds `body.sticky-bottom-footer`, and gem `_layout.scss:27` already zeroes `body{padding-bottom:70px}` for it. NO dead-space cleanup is needed — do not add a padding override. This is what stops the footer being a bar and makes it the bottom of the page (criterion 1).

`enable_darkmode` at line 474, `true` to `false`. `head.liquid:128` drops `theme.js`, the `initTheme()` call and the dark Pygments sheet; `header.liquid:127` drops the `#light-toggle` button. It does NOT remove the CSS dark block — that is why plan 02-02 wrote the `:root, html[data-theme="dark"]` selector list. Both halves are required; neither alone satisfies criterion 2.

`enable_progressbar` at line 478, `true` to `false`. Gates `header.liquid:141` (the `<span class="progress-bar">`) and `scripts.liquid:107` (`progress-bar.js`). The bar reads `--global-theme-color`.

Do NOT touch `max_width: 930px` at line 106 — the measure is Phase 3's decision (TYPE-04).

PurgeCSS ordering note, so nothing happens in the wrong order: `sticky-bottom` is ABSENT from today's served `main.css` because PurgeCSS stripped `footer.sticky-bottom` while no HTML used the class. Flipping the flag puts the class into the built HTML before PurgeCSS scans, so the rules come back on their own. Nothing to safelist — but never flip this flag and the footer CSS in separate deploys. They go in the same push, which this plan guarantees.

Then take the phase static-green, in this order:
1. `npx prettier _sass _config.yml assets/css/main.scss .al-folio-overrides.yml CLAUDE.md --check --end-of-line auto`
2. `node test/style_contract.js`
3. `bash .planning/phases/02-palette-and-design-tokens/verify.sh`

The entire static block must now be GREEN. Remove the `[red until plan 02-04]` label from each static row that is now true; the `--live` rows keep theirs until Task 2. If a static row is still red, fix its cause — do not delete the row.
  </action>
  <verify>
    <automated>grep -qE '^enable_darkmode:[[:space:]]*false' _config.yml && grep -qE '^enable_progressbar:[[:space:]]*false' _config.yml && grep -qE '^footer_fixed:[[:space:]]*false' _config.yml && grep -qE '^max_width:[[:space:]]*930px' _config.yml && node test/style_contract.js && npx prettier _sass _config.yml assets/css/main.scss .al-folio-overrides.yml CLAUDE.md --check --end-of-line auto && bash .planning/phases/02-palette-and-design-tokens/verify.sh</automated>
  </verify>
  <done>All three flags read `false`, `max_width` is untouched, Prettier and the style contract pass, and `verify.sh` (static, no `--live`) exits 0 with every row green.</done>
</task>

<task type="auto">
  <name>Task 2: Push once, then prove the repaint reached the CDN</name>
  <files>.planning/phases/02-palette-and-design-tokens/verify.sh</files>
  <action>
ONE `git push origin main`, fast-forward only, never `--force`. Everything from plans 02-01 through this task goes in that single push: the token file, `_custom.scss`, `assets/css/main.scss`, `.al-folio-overrides.yml`, `_config.yml`, `CLAUDE.md` and the planning docs. Splitting it is the failure mode this phase's hard ordering constraint exists to prevent.

Before pushing, confirm `git log --oneline origin/main..HEAD` contains the whole phase and nothing unexpected, and capture the FULL 40-character SHA of the pushed tip.

Then follow `docs/DEPLOYMENT.md`'s procedure — do not invent a new one:

1. Poll `https://api.github.com/repos/hkchi-pham/phamhakhanhchi.github.io/actions/runs?head_sha=<FULL-40-CHAR-SHA>` until the deploy run appears and concludes. Expect two runs (`deploy.yml` and `prettier.yml`), both green. `total_count: 0` with a full SHA within ~30s means the run genuinely did not fire — check `deploy.yml`'s denylist before assuming CDN lag.
2. Confirm `origin/gh-pages` advanced: `git ls-remote origin gh-pages`, compared against the value recorded in the SUMMARY of the previous phase.
3. Wait for the CDN (~124s from push), then run the live block:

       bash .planning/phases/02-palette-and-design-tokens/verify.sh --live

   Every `--live` row must go green: seven pages return 200; the served `main.css` contains `--paper-100`, `--global-bg-color: var(--paper-100)`, `--global-code-bg-color: var(--paper-200)`, `sticky-bottom`, and the exact `--deploy-proof` value plan 02-03 recorded; the served home HTML contains zero `light-toggle`, zero `fixed-bottom` and zero `progress-bar`.

   The served CSS is MINIFIED — write the greps with tolerant whitespace (`var(--paper-100)` may appear as `var(--paper-100)` with no space after the colon). If a row fails on whitespace rather than substance, fix the grep, note it in the SUMMARY, and re-run.

4. Remove the remaining `[red until plan 02-04]` labels once their rows are green.

Do NOT tag in this plan. Tagging (`design-NN-<slug>`, never `v*`) belongs to phase close-out, and STATE.md records that a tag must point at the commit that PRODUCED what is live — read off the `gh-pages` commit subject, not local HEAD.

If the deploy is red, do not retry blindly. `docs/DEPLOYMENT.md` §5 lists the three failure modes and the rollback path; the phase has a `design-00-baseline` tag to fall back to.
  </action>
  <verify>
    <automated>bash .planning/phases/02-palette-and-design-tokens/verify.sh --live</automated>
  </verify>
  <done>One push advanced `origin/main`; both workflow runs are green; `origin/gh-pages` advanced; `verify.sh --live` exits 0 with every static and live row green, including the served-CSS greps for `--paper-100`, `--global-code-bg-color: var(--paper-200)` and the new `--deploy-proof` value, and the zero-count greps for `light-toggle`, `fixed-bottom` and `progress-bar`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Seven-page sweep, dark-attribute force, greyscale link check</name>
  <action>Pause and hand the verification below to the user. Do not proceed past this task, and do not mark the plan or the phase complete, until they respond. Paste the seven live URLs and the one-line DevTools command into your message so they can click and copy straight through. Do not attempt to substitute a screenshot tool, Docker preview or the Playwright suite for this — none of them exists in a usable state on this machine, which is precisely why this checkpoint is here.</action>
  <what-built>
The whole phase is deployed. `_sass/_tokens.scss` holds every colour and length literal in the design with a measured ratio per colour and the vocabulary cap written alongside; all 30 of the gem's `--global-*` properties resolve through it under a `:root, html[data-theme="dark"]` selector list; `_sass/_custom.scss` carries the six overrides tokens cannot reach and no longer fades any text with `opacity`; `enable_darkmode`, `enable_progressbar` and `footer_fixed` are all `false`.

Everything mechanical is already asserted: `verify.sh --live` is green, which covers the token count, the cap, the ratio comments, the literal-free `_custom.scss`, the underline rule, the absence of the toggle/progress-bar/fixed footer in the served HTML, and — because no page on this site renders a code block — criterion 1's code-block clause as a served-CSS inspection (`--global-code-bg-color: var(--paper-200)`).

Three things remain that no tooling on this machine can do. That is not a gap in the plan: PITFALLS already establishes that no CI gate on this repo covers contrast, accessibility or appearance, and the Playwright suite is deliberately unusable here.
  </what-built>
  <how-to-verify>
Open these seven URLs, in order, in a normal browser window (the baseurl is EMPTY — no `/al-folio`):

  1. https://phamhakhanhchi.com/
  2. https://phamhakhanhchi.com/academics/
  3. https://phamhakhanhchi.com/research/
  4. https://phamhakhanhchi.com/further-reading/
  5. https://phamhakhanhchi.com/projects/
  6. https://phamhakhanhchi.com/activities/
  7. https://phamhakhanhchi.com/cv/

CHECK A — criterion 1, the repaint. On every page: is the warm cream ground behind the navbar, the body AND the footer? Specifically look for (a) any leftover WHITE panel — the likeliest place is the projects page's cards, which should now sit flush with the page and cast no shadow; (b) a DARK-GREY BAR pinned to the bottom — the gem's footer default is `#1c1c1d` and it should be gone entirely, with the footer now reading as the bottom of the page rather than a bar; (c) the CV page's embedded PDF object and its buttons.

CHECK B — criterion 2, dark is unreachable. First: is there a theme toggle anywhere? There should be none. Then on any page open DevTools console and run:

    document.documentElement.setAttribute('data-theme','dark')

NOTHING on the page may change colour. If the background goes dark, the selector list in `_sass/_tokens.scss` is not winning and this is a real failure — report it rather than approving. Reload to clear.

CHECK C — criterion 3, links survive greyscale. On the home page and one list page, take a full-page screenshot and view it in greyscale (DevTools > Rendering > Emulate vision deficiencies > Achromatopsia is the quickest route). Every link inside the body copy must still be obviously a link — underlined, hairline, sitting clear of the descenders and of the Vietnamese marks in "Phạm Hà Khánh Chi". Navbar and footer links are deliberately NOT underlined; that is correct, not a miss.

CHECK D — register, one sentence. Does the site read as a deliberate, tasteful recolour on warm paper, or does it read as broken/unfinished? Type is still the gem default at gem sizes — plain is expected at this stage; broken is not.
  </how-to-verify>
  <resume-signal>Type "approved", or describe exactly which page and which check failed.</resume-signal>
</task>

</tasks>

<verification>
- `bash .planning/phases/02-palette-and-design-tokens/verify.sh --live` exits 0, zero failures, zero `[red until …]` labels remaining.
- `curl -s "https://phamhakhanhchi.com/assets/css/main.css?cb=$(date +%s)" | grep -c -- "--paper-100"` is greater than 0.
- `curl -s https://phamhakhanhchi.com/ | grep -c "light-toggle"` returns 0.
- Human checkpoint returned "approved" for checks A, B, C and D.
- `.planning/REQUIREMENTS.md` traceability rows for TOKEN-01..06, GROUND-01 and GROUND-04 can be moved to Complete.
</verification>

<success_criteria>
All five phase success criteria are true on the deployed site: warm paper on all seven pages with no white panel and no dark bar; no toggle and an inert `data-theme="dark"`; body-copy links underlined and greyscale-identifiable; no `opacity` on text and the muted classes measured at 6.99:1; and one token file holding every literal, each colour with its ratio and the cap written beside it.
</success_criteria>

<output>
After completion, create `.planning/phases/02-palette-and-design-tokens/02-04-SUMMARY.md`. Record: the pushed 40-character SHA, both workflow run ids and conclusions, the `gh-pages` SHA before and after, the measured push-to-served-CSS latency, and the human checkpoint verdict per check (A/B/C/D). If the accent was swapped from blue to the oxblood fallback, record that and the reason.
</output>
