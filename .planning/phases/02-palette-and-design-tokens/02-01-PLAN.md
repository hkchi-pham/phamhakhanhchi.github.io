---
phase: 02-palette-and-design-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/tools/contrast.js
  - .planning/phases/02-palette-and-design-tokens/verify.sh
  - CLAUDE.md
autonomous: true
requirements: [TOKEN-01, TOKEN-03, TOKEN-06]

must_haves:
  truths:
    - "Any contrast ratio in this milestone can be recomputed by one command instead of picked by hand in DevTools."
    - "Every automatable criterion of Phase 2 can be re-checked by running one script."
    - "The 30 --global-* token names the site must re-point are recorded as ground truth read off the live CSS, not typed from memory."
    - "A future agent reading CLAUDE.md learns that this fork deliberately owns _sass/ before it deletes something."
  artifacts:
    - path: ".planning/tools/contrast.js"
      provides: "Zero-dependency WCAG 2.x relative-luminance contrast ratio CLI"
      min_lines: 20
      contains: "0.03928"
    - path: ".planning/phases/02-palette-and-design-tokens/verify.sh"
      provides: "Static + --live verification of TOKEN-01..06, GROUND-01, GROUND-04 and criteria 1-5"
      min_lines: 120
      contains: "--live"
    - path: "CLAUDE.md"
      provides: "Written note that the AGENTS.md stop sign is disabled in this fork"
      contains: "_sass"
  key_links:
    - from: ".planning/phases/02-palette-and-design-tokens/verify.sh"
      to: ".planning/tools/contrast.js"
      via: "node invocation inside a check row"
      pattern: "contrast\\.js"
    - from: ".planning/phases/02-palette-and-design-tokens/verify.sh"
      to: "_sass/_tokens.scss"
      via: "static greps over the 30-name token list"
      pattern: "--global-"
---

<objective>
Build the two pieces of verification infrastructure this phase needs before any colour changes, and record — in the one file a future agent is guaranteed to read — that this fork deliberately owns `_sass/`.

Purpose: 02-RESEARCH.md's Validation Architecture names three Wave 0 gaps: the contrast script, the `verify.sh` harness, and a written list of the 30 token names so a missed token fails loudly rather than silently staying purple. None exists. Without them, plans 02–04 have no `<automated>` verify and criterion 4's "each measure at least 4.5:1" degrades to manual DevTools work.

Output: `.planning/tools/contrast.js`, `.planning/phases/02-palette-and-design-tokens/verify.sh` (static block will be mostly RED until plans 02–04 land — that is correct), and a short fork note in `CLAUDE.md`.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-palette-and-design-tokens/02-CONTEXT.md
@.planning/phases/02-palette-and-design-tokens/02-RESEARCH.md
@.planning/phases/02-palette-and-design-tokens/02-VALIDATION.md

<environment>
- **No Ruby, no `bundle`, no `gem`, no `_site/`.** There is no local Jekyll build. Docker (`docker compose up -d` → `http://127.0.0.1:8080/`, **empty baseurl**, not `/al-folio`) is the only local preview route. The deployed site is the real check.
- **`gh` is absent, but the public GitHub REST API answers unauthenticated** (60 req/hr) for `hkchi-pham/phamhakhanhchi.github.io`.
- `core.autocrlf=true`. Run Prettier as `npx prettier <paths> --check --end-of-line auto`. **Never `npx prettier . --write`.**
- Surviving CI workflows: `deploy.yml`, `prettier.yml`, `broken-links-site.yml`. `unit-tests.yml`, `visual-regression.yml`, `upgrade-check.yml`, `axe.yml` were all deleted in Phase 1. Do not write steps that invoke them.
- **Do NOT run or update the Playwright visual suite** (`npm run test:visual*`). Its config hardcodes the `/al-folio` baseurl, targets deleted demo routes, and has no baseline for this site. Leaving it alone is the correct workflow — see 02-RESEARCH.md Pitfall 9.
- Git Bash mangles `git show <rev>:<path>`; prefix with `MSYS_NO_PATHCONV=1`. `$TMPDIR` is unset — write temp files to the session scratchpad.
- **Do NOT push in this plan.** Plan 02-04 owns the single push for this phase; `enable_darkmode: false` and the token re-point must reach the live site in one deploy.
</environment>

<harness_shape>
Copy the structure of `.planning/phases/01-deployment-guardrails/verify.sh` exactly:
`set -uo pipefail` (never `-e`), `cd "$(git rev-parse --show-toplevel)"`, a `check <label> <shell-expression>` function that increments `CHECKS`/`FAILED` and never aborts, a `--live` flag gating the network block, a final tally, and `exit 0` only when `FAILED` is 0.
It is deliberately NOT a test framework and deliberately NOT wired to an npm script — 01-VALIDATION forbids a starter-local test pipeline.
Rows that are not yet true stay in the script labelled `[red until plan 02-NN]` rather than being omitted, and the `EXPECTED_RED` tally counts them (same `case "$label"` idiom as Phase 1).
</harness_shape>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the zero-dependency WCAG contrast tool and self-validate it against PITFALLS.md</name>
  <files>.planning/tools/contrast.js</files>
  <action>
Create `.planning/tools/contrast.js` exactly as given in 02-RESEARCH.md §"Code Examples > The contrast tool" — WCAG 2.x relative luminance (`0.03928` / `12.92` / `2.4` gamma, `0.2126/0.7152/0.0722` coefficients), a `ratio(a,b)` using `(hi+0.05)/(lo+0.05)`, 3-digit hex expansion, and a CLI loop over `process.argv.slice(2)` taking pairs and printing `"#fg on #bg = N.NN:1  AA-text|AA-large/non-text|FAIL"`.

Add ONE thing beyond the research listing: a `--selftest` flag that runs the eight validated pairs below and exits non-zero if any computed value differs from the expected figure by more than 0.01. This turns "the tool is correct" into a check row rather than a claim.

    #2b2621 #faf6ee -> 13.90    #1f1b16 #faf6ee -> 15.88
    #5c5349 #faf6ee ->  6.99    #6b6259 #faf6ee ->  5.54
    #1d4ed8 #faf6ee ->  6.22    #9a3412 #faf6ee ->  6.78
    #1d4ed8 #2b2621 ->  2.24    #828282 #faf6ee ->  3.57

Zero dependencies. Node only (Volta node is on PATH). Do not add an npm script, a package.json entry, or a test framework.

No dependency install. No network.
  </action>
  <verify>
    <automated>node .planning/tools/contrast.js --selftest && node .planning/tools/contrast.js "#5c5349" "#faf6ee" | grep -q "6.99:1" && node .planning/tools/contrast.js "#d9cfba" "#faf6ee" | grep -q "FAIL"</automated>
  </verify>
  <done>`node .planning/tools/contrast.js --selftest` exits 0 and reproduces all eight PITFALLS.md figures to 0.01; `#d9cfba` on `#faf6ee` is reported FAIL (1.43:1), confirming `--rule-200` is decorative-only.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the 30 token names from the live CSS and build verify.sh around them</name>
  <files>.planning/phases/02-palette-and-design-tokens/verify.sh</files>
  <action>
**First, establish ground truth for the token list — do not type it from memory.** 02-RESEARCH.md Pitfall 2 records that ROADMAP, STACK and CONTEXT all say 29 and are wrong. Run:

    curl -s "https://phamhakhanhchi.com/assets/css/main.css?cb=$(date +%s)" \
      | grep -o -- '--global-[a-z0-9-]*' | sort -u

Expect exactly **30** distinct names. If the count is not 30, STOP and report — the gem may have been bumped off `al_folio_core` 1.0.15 and 02-RESEARCH.md's inventory would need re-reading. Paste the 30 names verbatim into a `GLOBAL_TOKENS` array near the top of `verify.sh` with a comment recording the date and that they were read off the served CSS.

Then write `.planning/phases/02-palette-and-design-tokens/verify.sh` using the harness shape in `<harness_shape>` above, with these check rows. Mark every row that cannot be true yet with `[red until plan 02-NN]` in its label.

STATIC BLOCK:
- TOKEN-01  `_sass/_tokens.scss` exists  `[red until plan 02-02]`
- TOKEN-01  each of the 30 `GLOBAL_TOKENS` names appears as a declaration (`^\s*--name:`) in `_sass/_tokens.scss` — one check row per name so a miss names itself  `[red until plan 02-02]`
- TOKEN-01  no gem partial is shadowed: for each of `_themes _variables _layout _typography _navbar _footer _blog _publications _components _utilities _tabs _teachings _typograms`, assert `_sass/_NAME.scss` does NOT exist
- TOKEN-01  `assets/css/main.scss` contains `@use "tokens";` and its line number is LESS than that of `@use "custom";`  `[red until plan 02-02]`
- TOKEN-01  `.al-folio-overrides.yml` `local_sha256` matches `sha256sum assets/css/main.scss`  `[red until plan 02-02]`
- TOKEN-02  exactly 4 `--step-` and exactly 4 `--space-` declarations in `_sass/_tokens.scss`  `[red until plan 02-02]`
- TOKEN-02  `_sass/_custom.scss` references `var(--space-` and `var(--step-`  `[red until plan 02-03]`
- TOKEN-03  `grep -rn "opacity" _sass/` returns zero hits — use criterion 4's own command verbatim  `[red until plan 02-03]`
- TOKEN-03  `node .planning/tools/contrast.js "#5c5349" "#faf6ee"` reports >= 4.5
- TOKEN-04  `grep -qE '^enable_darkmode:[[:space:]]*false' _config.yml`  `[red until plan 02-04]`
- TOKEN-04/05  `_sass/_tokens.scss` contains the two-line selector list `:root,` immediately followed by a line matching `html\[data-theme="dark"\]`  `[red until plan 02-02]`
- TOKEN-05  that same block declares `color-scheme: light`  `[red until plan 02-02]`
- TOKEN-06  the cap comment is present in `_sass/_tokens.scss` (grep for `paper tones`, `ink steps`, `accent colours`, `type families`)  `[red until plan 02-02]`
- TOKEN-06  declaration counts obey the cap: `--paper-` == 2, `--ink-` == 4, `--accent-` == 1, `--rule-` == 2 (count declarations only, `^\s*--x`, not `var()` references)  `[red until plan 02-02]`
- Criterion 5  every colour-primitive declaration line in `_sass/_tokens.scss` (`--paper-|--ink-|--accent-|--rule-`) carries a `:1` ratio in a trailing `/* */` comment  `[red until plan 02-02]`
- Criterion 5  `_sass/_custom.scss` contains NO colour or length literal: `grep -nE '#[0-9a-fA-F]{3,8}|[0-9]*\.?[0-9]+(rem|px|em)\b|color-mix' _sass/_custom.scss` returns nothing. Two documented exemptions, encoded as grep exclusions with a comment saying why: the `--deploy-proof` date string, and SCSS `//` comment lines.  `[red until plan 02-03]`
- GROUND-04  `_sass/_custom.scss` contains a `.post article a` rule with `text-decoration: underline`  `[red until plan 02-03]`
- Six gem overrides present in `_sass/_custom.scss` — one row each: `pre`/`code` ink, `.card { box-shadow: none }`, `.navbar { opacity: 1 }`, heading ink `h1..h6`, the body-copy underline, `:focus-visible` outline  `[red until plan 02-03]`
- Supporting  `enable_progressbar: false` and `footer_fixed: false` in `_config.yml`  `[red until plan 02-04]`
- Canary  `--deploy-proof` is still declared in `_sass/_custom.scss` (STATE.md: retained deliberately; a token audit must not delete it)
- Regression  `node test/style_contract.js` exits 0
- Regression  `npx prettier _sass _config.yml CLAUDE.md --check --end-of-line auto` exits 0
- Tool  `node .planning/tools/contrast.js --selftest` exits 0

LIVE BLOCK (`--live`, all `[red until plan 02-04]`):
- all seven pages return 200: `""`, `academics`, `research`, `further-reading`, `projects`, `activities`, `cv`
- served `main.css` (always with `?cb=$(date +%s)`) contains `--paper-100` and `--global-bg-color: var(--paper-100)` (allow flexible whitespace — the file is minified)
- served `main.css` contains `--global-code-bg-color:var(--paper-200)` — **this is how criterion 1's code-block clause is verified.** No page on this site renders a code block (02-RESEARCH.md Pitfall 12), so it is a CSS-inspection check, never a visual one.
- served `main.css` contains the new `--deploy-proof` value set in plan 02-03
- served `main.css` contains `sticky-bottom` (PurgeCSS restores those rules once `footer_fixed:false` puts the class into the HTML)
- served home HTML: `grep -c "light-toggle"` == 0, `grep -c "fixed-bottom"` == 0, `grep -c "progress-bar"` == 0

Include a header comment block, in the Phase 1 style, that (a) explains the red-by-design rows and forbids deleting them to get a clean tally, and (b) names the three genuinely manual-only criteria this script does NOT cover: the seven-page "no white panel / no dark bar" sweep, the DevTools `data-theme="dark"` force, and the greyscale link screenshot.

Do NOT add an npm script. Do NOT add a test framework.
  </action>
  <verify>
    <automated>bash .planning/phases/02-palette-and-design-tokens/verify.sh; test $? -ne 2 && grep -c '^check ' .planning/phases/02-palette-and-design-tokens/verify.sh | awk '$1>=40{exit 0}{exit 1}' && grep -q -- '--live' .planning/phases/02-palette-and-design-tokens/verify.sh && grep -c -- '--global-' .planning/phases/02-palette-and-design-tokens/verify.sh | awk '$1>=30{exit 0}{exit 1}'</automated>
  </verify>
  <done>`verify.sh` runs to completion, prints a PASS/FAIL line per row and a tally, lists all 30 `--global-*` names read off the live CSS, and exits non-zero with every red row labelled `[red until plan 02-NN]`. The regression rows (style contract, Prettier, contrast self-test) and the no-shadowed-gem-partial rows are GREEN already.</done>
</task>

<task type="auto">
  <name>Task 3: Record in CLAUDE.md that this fork owns _sass/</name>
  <files>CLAUDE.md</files>
  <action>
`AGENTS.md`'s stop sign lists `_sass/` among paths that "belong in a gem instead", and `CLAUDE.md` repeats that `npm run lint:style-contract` enforces it. Both statements are false for this fork and this phase is the first to create a *new* file under `_sass/` — so leaving them unreconciled invites a future agent to delete `_sass/_tokens.scss` as a contract violation.

Add a short section to `CLAUDE.md` — six lines at most, immediately after the intro paragraph that currently calls `AGENTS.md` authoritative — titled something like `## This fork: the stop sign is disabled`. It must state, plainly and with the evidence:

- `test/style_contract.js` carries its forbidden-path loop (lines 72–86) **commented out**, with an in-file explanation; `node test/style_contract.js` passes today with `_sass/` present.
- `unit-tests.yml`, the only workflow that ever ran that check, was deleted in Phase 1. The surviving workflows are `deploy.yml`, `prettier.yml`, `broken-links-site.yml`.
- `_sass/` is the site's own design surface here. `_sass/_tokens.scss` and `_sass/_custom.scss` are deliberate and must not be removed.
- This site's effective baseurl is **empty**, not `/al-folio`; the "Daily dev loop" and "Validated local command set" blocks describe the upstream demo.
- Authoritative for this site: `docs/DEPLOYMENT.md`, `.planning/PROJECT.md`, `.planning/STATE.md`.

Do **not** rewrite or delete the existing `AGENTS.md`/`CLAUDE.md` body. This is an additive reconciliation note, not a doc rewrite — the rest of the upstream text stays as the historical record, same convention STATE.md records for annotating rather than silently editing requirement text.

**Explicitly out of scope for Phase 2:** editing `AGENTS.md`, `docs/ARCHITECTURE.md` or `docs/BOUNDARIES.md`. Those carry the upstream thin-starter contract wholesale; re-writing them is a documentation milestone of its own, not token work, and no Phase 2 requirement covers it. The `CLAUDE.md` note above is the minimum that protects this phase's own output.

Run `npx prettier CLAUDE.md --write --end-of-line auto` afterwards, then re-read the added lines — STATE.md records Prettier mangling a load-bearing sentence in Phase 1 (it rewrites a `**`-terminated glob inside a bold span). Keep `_sass/**`-style globs out of bold spans.
  </action>
  <verify>
    <automated>grep -q "style_contract" CLAUDE.md && grep -q "_tokens.scss" CLAUDE.md && grep -qi "baseurl" CLAUDE.md && npx prettier CLAUDE.md --check --end-of-line auto</automated>
  </verify>
  <done>`CLAUDE.md` carries an additive note naming `test/style_contract.js`, the deleted `unit-tests.yml`, `_sass/_tokens.scss`, the empty baseurl and the authoritative files. Prettier passes. No existing line was deleted.</done>
</task>

</tasks>

<verification>
- `node .planning/tools/contrast.js --selftest` exits 0.
- `bash .planning/phases/02-palette-and-design-tokens/verify.sh` runs end to end, red rows all labelled `[red until plan 02-NN]`, zero unlabelled failures.
- `npx prettier _sass _config.yml CLAUDE.md --check --end-of-line auto` exits 0.
- `node test/style_contract.js` exits 0.
- `git status` shows no change under `_sass/`, `_config.yml` or `assets/`.
</verification>

<success_criteria>
Every `<automated>` command in plans 02-02, 02-03 and 02-04 has a tool that exists. The 30 token names are recorded from the served CSS, not from any document that says 29. A future agent reading `CLAUDE.md` will not delete `_sass/_tokens.scss`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-palette-and-design-tokens/02-01-SUMMARY.md`. Record the 30 token names verbatim and the date they were read off the live CSS.
</output>
