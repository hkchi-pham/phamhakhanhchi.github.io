---
phase: 01-deployment-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .prettierignore
  - .github/workflows/visual-regression.yml
  - .github/workflows/unit-tests.yml
  - .github/workflows/update-tocs.yml
  - .github/workflows/star-history.yml
  - .github/workflows/upgrade-check.yml
  - .github/workflows/render-cv.yml
  - .github/workflows/docker-slim.yml
  - .github/workflows/deploy-image.yml
  - .github/workflows/deploy-docker-tag.yml
  - .github/workflows/broken-links.yml
  - .github/workflows/lighthouse-badger.yml
  - .github/workflows/release.yml
  - .github/workflows/update-screenshots.yml
  - .github/workflows/copilot-setup-steps.yml
  - .github/workflows/prettier-html.yml
  - .github/workflows/prettier-comment-on-pr.yml
  - .github/workflows/axe.yml
  - .github/workflows/codeql.yml
  - .github/workflows/update-citations.yml
autonomous: true
requirements: [SAFE-03]

must_haves:
  truths:
    - "No PR touching _sass/**, _pages/** or assets/** can show a visual-regression check, because the workflow file no longer exists."
    - "Pushing this repo's .planning/** documents does not turn prettier.yml red."
    - "No workflow left in .github/workflows/ auto-commits to main."
    - "Every workflow file left in the repo is capable of passing on this site."
  artifacts:
    - path: ".prettierignore"
      provides: "Prettier exclusion for agent-authored planning docs"
      contains: ".planning/**"
    - path: ".github/workflows/deploy.yml"
      provides: "The single deploy path (untouched by this plan, edited by plan 02)"
    - path: ".github/workflows/prettier.yml"
      provides: "The one inherited check that genuinely applies here"
    - path: ".github/workflows/broken-links-site.yml"
      provides: "Deployed-site link check, used again in Phase 8"
  key_links:
    - from: ".github/workflows/"
      to: "GitHub Actions checks on a PR"
      via: "file presence — a deleted workflow cannot report a check"
      pattern: "^(broken-links-site|deploy|prettier)\\.yml$"
---

<objective>
Reduce `.github/workflows/` from 22 inherited al-folio files to the 3 that can actually pass on this site, and stop `prettier.yml` from going red on this project's own planning documents.

Purpose: Criterion 3 of the phase ("a PR touching `_sass/**`, `_pages/**` or `assets/**` shows no `visual-regression` check") becomes structurally true rather than filter-dependent. At the same time this removes `update-tocs.yml`, the only live auto-committer, which would otherwise rewrite and auto-commit this phase's own `docs/DEPLOYMENT.md` deliverable — so it MUST land in a commit earlier than the one that creates that doc.

Output: a pruned workflow directory (3 files), and a `.prettierignore` that keeps CI green from the first push of this milestone onward.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deployment-guardrails/01-CONTEXT.md
@.planning/phases/01-deployment-guardrails/01-RESEARCH.md

<research_corrections>
Two premises carried in CONTEXT.md are STALE. Do not repeat them in commit messages:

1. `unit-tests.yml` does NOT currently fail. Commit `81e55bd` already commented out the forbidden-path block in `test/style_contract.js` (the block that used to reject `_sass/`). It passes today and would keep passing through Phases 2-8. It is still deleted here — but for **irrelevance**, not failure: everything it still asserts (`theme: al_folio_core`, the five plugin ids, three SRI hashes, the `al_math` exact-version pin) guards the *upstream thin-starter boundary*, which is not a property this personal site cares about. Under this phase's own standard — "a green tick must mean something" — a check that only guards someone else's invariant should not be wired up. Say exactly that in the commit message.

2. `star-history.yml` is NOT a live auto-commit risk. It carries a job-level `if: github.repository == 'alshedivat/al-folio'` guard, so its job is skipped on this fork. Same for `broken-links.yml`, `deploy-image.yml`, `release.yml` and `update-screenshots.yml`. They are deleted for **noise**, not danger. `update-tocs.yml` is the only one with `contents: write` and no repository guard — it is the real hazard.
</research_corrections>

<current_state>
22 files in `.github/workflows/` (plus `schedule-posts.txt`, which is not a workflow — GitHub ignores it, leave it alone).

`.prettierignore` today:
```
**/*.map
**/*.min.css
**/*.min.js
assets/plotly/demo.html
lighthouse_results/**
_posts/2015-10-20-math.md
# Ignore citation YAML file generated by script
_data/citations.yml
```

`prettier.yml` runs on **every** push to `main` with no path filter. 17 files currently fail `npx prettier . --check --end-of-line auto`, and all 17 are under `.planning/`. There are 12 unpushed commits on local `main`, all of them `.planning/**`. Without this plan's `.prettierignore` line, the first push of this phase turns `main` red.

**Windows CRLF trap:** `core.autocrlf=true` here. `npx prettier . --check` (without `--end-of-line auto`) reports 99 false failures and `npx prettier . --write` would rewrite 99 files with line-ending-only churn. ALWAYS pass `--end-of-line auto` locally. Never run a bare `npx prettier . --write` in this plan.
</current_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stop prettier.yml going red on agent-authored planning docs</name>
  <files>.prettierignore</files>
  <action>
Append a `.planning/**` entry to `.prettierignore`, with a one-line comment above it explaining why (GSD writes these docs every phase; reformatting them forever is churn, and `prettier.yml` has no path filter so they gate every push).

Place it after the existing `lighthouse_results/**` line, keeping the file's existing style. Suggested text:

```
# Agent-authored planning documents — regenerated every phase, not source.
.planning/**
```

Then confirm the whole repo is Prettier-clean the way CI sees it. Run:

```bash
npx prettier . --check --end-of-line auto
```

Expect "All matched files use Prettier code style!". If any file OUTSIDE `.planning/` still fails, fix that one file individually with `npx prettier --write <path>` — do NOT run `npx prettier . --write`, which would rewrite 99 files' line endings on this CRLF checkout (Pitfall 5 in RESEARCH.md).
  </action>
  <verify>
    <automated>npx prettier . --check --end-of-line auto</automated>
    <automated>grep -q '^\.planning/\*\*$' .prettierignore</automated>
  </verify>
  <done>`npx prettier . --check --end-of-line auto` exits 0 with zero failing files, and `.prettierignore` contains a `.planning/**` line.</done>
</task>

<task type="auto">
  <name>Task 2: Prune the 19 inherited workflows that cannot pass or should not run here</name>
  <files>.github/workflows/visual-regression.yml, .github/workflows/unit-tests.yml, .github/workflows/update-tocs.yml, .github/workflows/star-history.yml, .github/workflows/upgrade-check.yml, .github/workflows/render-cv.yml, .github/workflows/docker-slim.yml, .github/workflows/deploy-image.yml, .github/workflows/deploy-docker-tag.yml, .github/workflows/broken-links.yml, .github/workflows/lighthouse-badger.yml, .github/workflows/release.yml, .github/workflows/update-screenshots.yml, .github/workflows/copilot-setup-steps.yml, .github/workflows/prettier-html.yml, .github/workflows/prettier-comment-on-pr.yml, .github/workflows/axe.yml, .github/workflows/codeql.yml, .github/workflows/update-citations.yml</files>
  <action>
Delete these 19 files with `git rm`. Deletion, not stripping to `workflow_dispatch:` — CONTEXT's default, and five of them are already dispatch-only so stripping would gain nothing:

```bash
git rm .github/workflows/visual-regression.yml \
       .github/workflows/unit-tests.yml \
       .github/workflows/update-tocs.yml \
       .github/workflows/star-history.yml \
       .github/workflows/upgrade-check.yml \
       .github/workflows/render-cv.yml \
       .github/workflows/docker-slim.yml \
       .github/workflows/deploy-image.yml \
       .github/workflows/deploy-docker-tag.yml \
       .github/workflows/broken-links.yml \
       .github/workflows/lighthouse-badger.yml \
       .github/workflows/release.yml \
       .github/workflows/update-screenshots.yml \
       .github/workflows/copilot-setup-steps.yml \
       .github/workflows/prettier-html.yml \
       .github/workflows/prettier-comment-on-pr.yml \
       .github/workflows/axe.yml \
       .github/workflows/codeql.yml \
       .github/workflows/update-citations.yml
```

Keep exactly three: `deploy.yml`, `prettier.yml`, `broken-links-site.yml`. Leave `schedule-posts.txt` alone (not a workflow).

Do NOT touch `test/visual/` or the `test:visual*` npm scripts — CONTEXT does not ask for their removal and a future TEST-01 milestone may retarget them.

Write the commit message so a reader six months from now knows why each group went. Group the reasoning, do not list 19 one-liners:

- `visual-regression.yml` — specs target `/al-folio/` routes and v0.16.3 demo content this site does not have; it can never pass. Deleting it is what makes phase criterion 3 structurally true.
- `update-tocs.yml` — auto-commits to `main` with `contents: write` and no repository guard; it matches `docs/*.md`, which is where this phase's `docs/DEPLOYMENT.md` lands. A bot commit mid-phase causes non-fast-forward push rejections.
- `unit-tests.yml` — **passes today** (see `<research_corrections>`); removed because its assertions guard the upstream thin-starter boundary, not this site. A green tick that means nothing is worse than no tick.
- `star-history.yml`, `broken-links.yml`, `deploy-image.yml`, `release.yml`, `update-screenshots.yml` — job-level `if: github.repository == 'alshedivat/al-folio'` means their jobs are already skipped on this fork. Removed for **noise**, not danger.
- `upgrade-check.yml`, `render-cv.yml`, `update-citations.yml` — need `bundle exec al-folio`, `rendercv[full]` and `scholarly` + a Scholar ID, none of which are wired up here.
- `docker-slim.yml`, `deploy-image.yml`, `deploy-docker-tag.yml` — publish upstream's container images.
- `axe.yml`, `lighthouse-badger.yml`, `prettier-html.yml` — already `workflow_dispatch`-only; `lighthouse-badger.yml` measures the upstream demo, not this site.
- `copilot-setup-steps.yml` — triggers only on its own file.
- `codeql.yml` — code scanning on a static Jekyll site is noise.
- `prettier-comment-on-pr.yml` — only fires on a `repository_dispatch` that `prettier.yml` sends when a PR check fails. Note in the commit message that `prettier.yml` now dispatches into nothing; this is harmless, because that dispatch step sits inside a `failure()` guard and a failed dispatch cannot un-fail an already-red job.

Do not touch `.github/workflows/deploy.yml` in this plan — plan 02 owns that file.
  </action>
  <verify>
    <automated>test "$(ls .github/workflows/*.yml | xargs -n1 basename | sort | tr '\n' ' ')" = "broken-links-site.yml deploy.yml prettier.yml "</automated>
    <automated>test ! -f .github/workflows/visual-regression.yml && test ! -f .github/workflows/update-tocs.yml && test ! -f .github/workflows/unit-tests.yml</automated>
    <automated>! grep -rl "contents: write" .github/workflows/ | grep -qv deploy.yml</automated>
  </verify>
  <done>`.github/workflows/` contains exactly `broken-links-site.yml`, `deploy.yml` and `prettier.yml` (plus the non-workflow `schedule-posts.txt`). No remaining workflow auto-commits to `main`. The commit message states the corrected reason for removing `unit-tests.yml` (irrelevance, not failure) and for `star-history.yml` (noise, not danger).</done>
</task>

</tasks>

<verification>
1. `ls .github/workflows/*.yml` lists exactly three files.
2. `npx prettier . --check --end-of-line auto` exits 0.
3. `grep -rn "github-actions\[bot\]\|contents: write" .github/workflows/` shows nothing that can push to `main` other than `deploy.yml`'s publish of `gh-pages`.
4. Nothing under `_sass/`, `_pages/`, `assets/` or `docs/` was modified by this plan — `git show --stat HEAD~1..HEAD` touches only `.prettierignore` and `.github/workflows/`.
</verification>

<success_criteria>
- SAFE-03 is structurally satisfied: `visual-regression.yml` does not exist, so no PR can display that check regardless of path filters.
- The only auto-committer on `main` is gone, so this phase's own `docs/DEPLOYMENT.md` (plan 05) cannot be rewritten by a bot.
- Prettier passes locally the way it will in CI, so the phase's first push does not go red.
- Every surviving workflow is capable of passing on this repository.
</success_criteria>

<output>
After completion, create `.planning/phases/01-deployment-guardrails/01-01-SUMMARY.md`.
Record: the exact final workflow inventory, the prettier check result, and a one-line note that the `unit-tests.yml` premise correction was applied.
</output>
