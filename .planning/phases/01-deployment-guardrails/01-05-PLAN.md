---
phase: 01-deployment-guardrails
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - docs/DEPLOYMENT.md
autonomous: false
requirements: [SAFE-03, SAFE-04]

must_haves:
  truths:
    - "A known-good pre-redesign commit is tagged on the remote and it is the commit that produced what is currently live."
    - "Someone who has never seen this repo can roll back a bad design commit by following a written procedure."
    - "The doc states that a revert touching only _sass/** now redeploys — and why that was false before this phase."
    - "The three silent failure modes each have a symptom, a confirm command and a fix written down."
    - "The GitHub Pages settings and branch-protection state are recorded, so deleting visual-regression.yml cannot leave a PR stuck on an 'Expected' required check."
  artifacts:
    - path: "docs/DEPLOYMENT.md"
      provides: "Rollback procedure, tag list, Pages settings snapshot, the three silent failure modes"
      min_lines: 70
      contains: "design-00-baseline"
  key_links:
    - from: "docs/DEPLOYMENT.md"
      to: "git tag design-00-baseline"
      via: "the tag list section naming the SHA and what was live at it"
      pattern: "design-00-baseline"
    - from: "docs/DEPLOYMENT.md"
      to: ".planning/phases/01-deployment-guardrails/verify.sh"
      via: "the by-hand verification section pointing at the script"
      pattern: "verify\\.sh"
---

<objective>
Close the phase: tag the known-good pre-redesign commit, read the GitHub Pages and branch-protection settings out of the browser and record them, and write `docs/DEPLOYMENT.md` — the way back.

Purpose: everything else in this phase is machinery. This is the part a person reads at 11pm when the site looks wrong. It also closes the one SAFE-03 loose end that no command on this machine can reach: if `visual-regression` or `style-contract` is still configured as a **required status check** in repo settings, deleting the workflow leaves PRs permanently stuck on "Expected — waiting for status to be reported", which is worse than a red X.

Output: `design-00-baseline` on `origin`, and `docs/DEPLOYMENT.md` on `main`.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-deployment-guardrails/01-CONTEXT.md
@.planning/phases/01-deployment-guardrails/01-RESEARCH.md
@.planning/phases/01-deployment-guardrails/01-03-SUMMARY.md
@.planning/phases/01-deployment-guardrails/01-04-SUMMARY.md

<facts_the_doc_must_state_correctly>
- **The baseline commit is `778f68b`**, not local HEAD. `origin/gh-pages@f7b878d` says "Deploying to gh-pages from ...@778f68b", so `778f68b` is what produced the site that was live at the start of this phase. Everything between it and the phase's work is planning docs and guardrails — not a byte of the rendered site.
- **Pushing a tag does not trigger `deploy.yml`** — its `on.push` has `branches:` only, no `tags:`. Separately, the upstream `deploy-docker-tag.yml` fired on `v*` tags; it has been deleted, but the `design-*` scheme avoids that collision anyway. Do not switch the scheme to `v*` without re-checking.
- **The 2026-09-10 incident post-mortem, stated accurately:** the two deploys that day (`a608b18` from `545d014`, `aa91529` from `c146db5`) both predate `a5dd00f`, the commit that first put `CNAME` on `main`. The root cause was "CNAME was not yet in the source", not "the deploy deleted it". `ed4c9cf` "Delete CNAME" / `ca2d479` "Create CNAME" on `gh-pages` was the manual rescue. An accurate timeline is worth more than a dramatic one — and the new pre-deploy assertion does catch that exact cause.
- **CNAME is currently healthy.** Three deploys ran on 2026-09-11 after `CNAME` landed on `main`, and `origin/gh-pages:CNAME` is byte-identical to `main:CNAME`. The guard is regression insurance, not a repair.
- **`JamesIves/github-pages-deploy-action@v4` already protects a *missing* CNAME**: when `_site/CNAME` is absent it adds `--exclude CNAME` to its `rsync --delete`, so the branch copy survives. The uncovered hazard the new guard actually closes is a **present-but-wrong** `_site/CNAME` (empty, truncated, or a different hostname) being rsynced over the good one.
- **Two path-filter limits worth one line each**, because they are genuinely silent: a diff of more than 3,000 files may not match path filters at all, and a push of more than 1,000 commits bypasses path filtering entirely.
- **Served assets carry `Cache-Control: max-age=600` behind a CDN** — the reason every "did it ship?" curl needs `?cb=$(date +%s)`.
- **`.nojekyll` is absent** from both the repo root and `gh-pages`, so GitHub runs its own Jekyll pass on the published branch and drops underscore-prefixed directories (`https://phamhakhanhchi.com/_pages/dropdown/` 404s). Nothing the site links to is affected. Record it as a known quirk with the one-line fix; do NOT fix it in this phase.
</facts_the_doc_must_state_correctly>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Read the GitHub Pages and branch-rule settings in a browser</name>
  <action>Pause here BEFORE writing any file in this plan — task 2 records these values and must not invent them. Hand the user the read-only inspection below and wait for their answer.</action>
  <what-built>
Everything automatable in this phase is done and verified. Two facts remain that cannot be read from this machine: `gh` is not installed and the GitHub REST API needs auth, so repository settings are browser-only. They are needed before `docs/DEPLOYMENT.md` can be written, because the doc records them.
  </what-built>
  <how-to-verify>
Open the repository settings in a browser and report these values verbatim. **Read only — change nothing.**

1. **Settings → Pages**
   - Source: which branch and which folder? (expected: `gh-pages` / `/ (root)`)
   - Custom domain: exact value shown, and whether the DNS-check badge is green.
   - Enforce HTTPS: checked or unchecked?
   - Any warning banner shown on that page.

2. **Settings → Rules → Rulesets**, and **Settings → Branches** (classic branch protection)
   - Is there ANY rule or protection targeting `main`?
   - If yes: does it list **required status checks**? Name every one.
   - **If `visual-regression`, `unit-tests`, `style-contract` or any other now-deleted workflow is listed as required, remove that requirement now** and say so. Leaving it set makes every future PR hang on "Expected — waiting for status to be reported", which is worse than the red X this phase set out to remove. This is the one SAFE-03 loose end no command here can reach.
   - If no rule targets `main` at all, say "none" — that is the expected answer for a solo direct-push project and is equally worth recording.
  </how-to-verify>
  <resume-signal>Paste the Pages values and the branch-rule answer (or "none"), plus a note if you removed a stale required check.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Tag the pre-redesign baseline on the remote</name>
  <files>(no files modified — creates and pushes an annotated git tag)</files>
  <action>
Tag `778f68b` — the commit that produced the site that was live at the start of this phase — not local HEAD.

First confirm the premise still holds, so the tag message is true rather than copied:

```bash
git fetch origin --prune --tags
git log -1 --format='%H %s' 778f68b
git log --format=%s origin/gh-pages | grep -m1 778f68b    # the "Deploying ... from ...@778f68b" line
```

Then create and push the annotated tag. Write the message so it answers "what was the site like here?" without needing the repo open:

```bash
git tag -a design-00-baseline 778f68b -m "Pre-redesign baseline.

Deployed as gh-pages@f7b878d on 2026-09-11; phamhakhanhchi.com verified 200.
Site is gem-default al-folio styling: white ground, gem default type, no
design-pass changes. Everything after this tag is the portfolio design
language milestone.

Roll back to this state with:  git revert --no-edit <oldest-bad>^..<newest-bad>
Never reset or force-push main."
git push origin design-00-baseline
```

Pushing a tag does not trigger `deploy.yml` (its `on.push` lists `branches:` only). Confirm nothing deployed by comparing `git ls-remote origin refs/heads/gh-pages` before and after — it must be unchanged.

Establish the scheme for the rest of the milestone, and note it in the SUMMARY so later phases follow it: one annotated tag per phase completion, `design-NN-<slug>` (`design-02-tokens`, `design-03-type`, ...), always `design-*` and never `v*`.
  </action>
  <verify>
    <automated>git ls-remote --tags origin | grep -q 'refs/tags/design-00-baseline$'</automated>
    <automated>test "$(git rev-list -n1 design-00-baseline)" = "$(git rev-parse 778f68b)" && git tag -l --format='%(contents:subject)' design-00-baseline | grep -qi 'baseline'</automated>
  </verify>
  <done>An annotated tag `design-00-baseline` exists on `origin` pointing at `778f68b`, its message states what the site looked like there and how to get back, and the tag push triggered no deploy (`gh-pages` SHA unchanged).</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/DEPLOYMENT.md and push it</name>
  <files>docs/DEPLOYMENT.md</files>
  <action>
Write `docs/DEPLOYMENT.md`. This is site-specific operations prose — deliberately NOT in `.planning/` (it outlives the milestone) and deliberately distinct from the inherited upstream al-folio docs sitting beside it. Open with one line saying so, because every other file in `docs/` describes the upstream demo and is not about this site.

Prerequisite already satisfied: `update-tocs.yml` was deleted in plan 01. It triggered on `docs/*.md` with `contents: write` and would have rewritten this file and auto-committed to `main`. Verify before writing: `test ! -f .github/workflows/update-tocs.yml`.

Sections, in this order. Substitute real values from the checkpoint and from the 01-03 / 01-04 SUMMARYs — no placeholders left in the committed file.

**1. How a change reaches the live site.** The chain: `main` → `.github/workflows/deploy.yml` → `bundle exec jekyll build` → PurgeCSS → `JamesIves/github-pages-deploy-action@v4` → `gh-pages` → GitHub Pages → `phamhakhanhchi.com`. State plainly what triggers a deploy: **everything except** the `paths-ignore` denylist, with the list reproduced. State the two consequences that surprise people: a change to `.github/**` does not deploy (use `workflow_dispatch`), and a push mixing an ignored file with a non-ignored one *does* deploy, because the run is skipped only when ALL changed paths match.

**2. GitHub Pages settings snapshot (verified <date>, read-only).** Source branch/folder, custom domain, DNS check state, Enforce HTTPS, and the branch-rule / required-status-check answer from the checkpoint. Note that these are repository *settings*, a separate piece of state from the `CNAME` files in `main` and `gh-pages` — drift between the three is its own failure mode.

**3. Tags.** The scheme (`design-NN-<slug>`, one per phase completion, never `v*`) and the fact that tag pushes do not deploy. One line per tag; today that is `design-00-baseline` → `778f68b`, pre-redesign, live as of 2026-09-11.

**4. Rolling back.** Three recipes, each a copy-pasteable block:
   - one bad commit: `git revert --no-edit <sha> && git push origin main`
   - a whole phase: `git revert --no-edit <oldest>^..<newest> && git push origin main`
   - back to a tag: how to identify the range from `git log design-0N-slug..main`
   Then the sentence SAFE-04 exists for, stated explicitly and with its history: **a revert touching only `_sass/**` now redeploys automatically. Before this phase it would not have — `deploy.yml`'s push filter was an allowlist that never listed `_sass/**`, so a `_sass`-only commit (and therefore a `_sass`-only revert) produced no workflow run at all, silently.** Add the two hard rules: never `git push --force` to `main`; never hand-edit `gh-pages` (the next deploy's `rsync --delete` erases it — the sole exception being `CNAME`/`.nojekyll` when absent from `_site`).

**5. Three failures that produce no error message.** Each with *Symptom* / *How to confirm* / *Fix*:
   1. **The change never deployed — path filter.** Symptom: the commit is on `main`, the site is unchanged, and there is no red X because there was no run. Confirm: `git ls-remote origin refs/heads/gh-pages` has not moved, and no Deploy site run exists for that SHA. Fix: check the changed paths against `paths-ignore`; re-run via `workflow_dispatch`. Add the two silent scale limits here — diffs over 3,000 files may not match path filters, and pushes over 1,000 commits bypass them entirely.
   2. **The domain dropped — CNAME.** Symptom: the site resolves at `phamhakhanhchi.github.io` but the custom domain 404s or shows a certificate error. Confirm: `git cat-file -p origin/gh-pages:CNAME` and Settings → Pages. Fix: restore `CNAME` at the repo root and redeploy. State what is and is not covered: the deploy action already protects a *missing* `_site/CNAME` (it excludes CNAME from `rsync --delete` when the source has none); the pre-deploy assertion in `deploy.yml` covers the case the action does not — a present-but-wrong `_site/CNAME` overwriting the good one. Include the accurate 2026-09-10 timeline from `<facts_the_doc_must_state_correctly>`.
   3. **A CSS rule vanished — PurgeCSS.** Symptom: it looks right on `jekyll serve` and wrong in production. Confirm: grep the served CSS with a cache-buster. Fix: add the selector to `purgecss.config.js`'s safelist. Note the `max-age=600` CDN cache, and that this is exactly what Phase 8 audits by hand.

**6. Verifying a deploy by hand.** The commands, ready to paste:
```bash
curl -sI https://phamhakhanhchi.com | head -3
curl -s "https://phamhakhanhchi.com/assets/css/main.css?cb=$(date +%s)" | grep -c -- "--deploy-proof"
git ls-remote origin refs/heads/gh-pages | cut -f1
bash .planning/phases/01-deployment-guardrails/verify.sh --live
```
Explain the `--deploy-proof` canary in `_sass/_custom.scss`: re-date it, push a `_sass`-only commit, and grep the served CSS — that answers "did my change ship?" without guessing. Record the realistic timings measured in plan 04 (how long `gh-pages` took to advance, how long the CSS took to turn over) so a future reader knows when to start worrying.

**7. Known quirks.** `.nojekyll` is absent from the repo root and from `gh-pages`, so GitHub's own Jekyll pass drops underscore-prefixed directories on the published branch (`/_pages/dropdown/` 404s). Nothing the site links to is affected; a one-line `.nojekyll` at the repo root would close it (it is already in `_config.yml`'s `keep_files:`). Explicitly out of scope for this phase.

Finally: Prettier-check the new file the way CI will, commit, and push. `docs/**` is in `paths-ignore`, so this push must NOT deploy — confirm `gh-pages` is unchanged afterwards, which is itself a last small proof that the denylist works.

```bash
npx prettier docs/DEPLOYMENT.md --check --end-of-line auto
git add docs/DEPLOYMENT.md && git commit -m "docs(01-05): write the deployment and rollback procedure"
git log --oneline main..origin/main    # must be empty
git push origin main
```
  </action>
  <verify>
    <automated>test -f docs/DEPLOYMENT.md && grep -qi "path filter" docs/DEPLOYMENT.md && grep -qi "CNAME" docs/DEPLOYMENT.md && grep -qi "PurgeCSS" docs/DEPLOYMENT.md && grep -q "design-00-baseline" docs/DEPLOYMENT.md</automated>
    <automated>grep -q '_sass' docs/DEPLOYMENT.md && grep -qi 'revert' docs/DEPLOYMENT.md && grep -qi 'Enforce HTTPS' docs/DEPLOYMENT.md && grep -q 'verify.sh' docs/DEPLOYMENT.md</automated>
    <automated>test "$(grep -c . docs/DEPLOYMENT.md)" -ge 70 && ! grep -qE '\{|YYYY-MM-DD|TBD|TODO' docs/DEPLOYMENT.md</automated>
    <automated>npx prettier . --check --end-of-line auto</automated>
    <automated>git fetch origin --prune >/dev/null 2>&1; git show origin/main:docs/DEPLOYMENT.md | grep -q 'design-00-baseline'</automated>
  </verify>
  <done>`docs/DEPLOYMENT.md` is on `origin/main`, contains all seven sections with real values (no placeholders), names `design-00-baseline` and its SHA, states explicitly that a `_sass`-only revert now redeploys and that it did not before, and gives symptom/confirm/fix for all three silent failure modes. The push did not trigger a deploy (`docs/**` is ignored), confirmed by an unchanged `gh-pages` SHA.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Have a human read docs/DEPLOYMENT.md for the two prose claims</name>
  <action>Pause and hand the read-through below to the user. This is the phase's last gate: two of phase criterion 4's requirements are prose that no grep can validate. Do not mark the phase complete without their answer; if they list sentences to fix, fix them and push before re-asking.</action>
  <what-built>
`design-00-baseline` is tagged on `origin` at `778f68b`, and `docs/DEPLOYMENT.md` is on `main` with the rollback procedure, the tag list, the Pages settings snapshot and the three silent failure modes.

Two things in this plan are prose claims that no grep can validate. `01-VALIDATION.md` lists both as manual-only: a `grep -q "_sass"` proves the word appears, not that the document says the right thing about it.
  </what-built>
  <how-to-verify>
Read `docs/DEPLOYMENT.md` end to end — it should take about five minutes — and answer:

1. **Section 4 (Rolling back):** does it state clearly that a revert touching only `_sass/**` now redeploys automatically, AND that this was false before this phase? This is phase success criterion 4 and it is a prose claim, not a grep. If the sentence is hedged or missing, say so.
2. **Section 5 (Three silent failures):** for each of the three, could you actually follow it at 11pm with the site looking wrong — a symptom you would recognise, a command to confirm it, and a fix?
3. **Section 2:** do the Pages values match what you read in the browser during the earlier checkpoint?
4. Are there any leftover placeholders, `<sha>`-style stubs, or claims you know to be wrong?

Optionally rehearse the rollback without executing it: pick the `--deploy-proof` commit and confirm the doc tells you exactly what to type.
  </how-to-verify>
  <resume-signal>Type "approved", or list the sentences that need fixing.</resume-signal>
</task>

</tasks>

<verification>
1. `git ls-remote --tags origin | grep design-00-baseline` — the tag is on the remote, pointing at `778f68b`.
2. `bash .planning/phases/01-deployment-guardrails/verify.sh --live` — **every** row passes, including both SAFE-04 rows, which were the only outstanding failures after plan 04.
3. `git show origin/main:docs/DEPLOYMENT.md` exists and contains no placeholders.
4. `git ls-remote origin refs/heads/gh-pages` is unchanged across both the tag push and the docs push — `docs/**` and tags are correctly outside the deploy trigger.
5. `curl -sI https://phamhakhanhchi.com | head -1` still shows 200.
6. Human has read the doc and confirmed the criterion-4 sentence and the three failure-mode entries are genuinely usable.
</verification>

<success_criteria>
- **Phase criterion 4 is met:** a known-good commit is tagged (`design-00-baseline` → `778f68b`, the commit that produced what was live), the rollback procedure is written down, and the doc states explicitly that a `_sass`-only revert now redeploys because of criterion 1 — with the history of why that was false before.
- The SAFE-03 loose end no command on this machine could reach is closed: branch rules were inspected in the browser and any required status check naming a deleted workflow was removed, so no PR can hang on "Expected".
- The GitHub Pages settings are recorded rather than assumed, removing the unknown without changing anything.
- `verify.sh --live` is fully green, so the phase has a single command that re-proves every automatable criterion at any future date.
</success_criteria>

<output>
After completion, create `.planning/phases/01-deployment-guardrails/01-05-SUMMARY.md`.
Record: the Pages settings values as read, the branch-rule answer (and whether a stale required check was removed), the tag SHA and message, the final full-green `verify.sh --live` output, and the human checkpoint outcome.

Then update `.planning/phases/01-deployment-guardrails/01-VALIDATION.md`: tick the four manual-only rows and set `status: complete`.
</output>